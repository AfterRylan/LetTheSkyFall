#include <algorithm>
#include <vector>
#include <iostream>
using namespace std;

int main() {

    int n;
    cin >> n;
    std::vector<int> AddVec(std::vector<int> & vec, int n); {
        for (int i = 0; i < vec.size; i++)
            vec[i] += n;
    }
    return vec;
}